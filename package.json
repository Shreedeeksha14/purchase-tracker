{
  "name": "purchase-tracker",
  "version": "1.0.0",
  "description": "Simple Node.js + Express + MySQL app to log item purchases and view them in a table with JOIN-based lookups.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mysql2": "^3.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
